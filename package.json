{
  "name": "credit-union",
  "version": "1.0.0",
  "description": "On-chain decentralized credit union with tiered collateral and reputation tracking",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "serve": "npx serve frontend -p 3000"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.26.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  }
}
